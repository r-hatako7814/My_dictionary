      <div class = "mt-1 ml-3" style = "width: 30%; text-decoration:underline;">お名前</div>
      <div class = "mt-1 ml-3"><%= @contact.name %>  様</div>

      <div class="mt-3 ml-3" style="width: 30%; text-decoration:underline;">返信用メールアドレス</div>
      <div class = "mt-1 ml-3"><%= @contact.email %></div>

      <div class="mt-3 ml-3" style="width: 30%; text-decoration:underline;">電話番号</div>
      <div class = "mt-1 ml-3"><%= @contact.phone_number %></div>
      
      <div class="mt-3 ml-3" style="width: 30%; text-decoration:underline;">問い合わせ項目</div>
      <div class="mt-3 ml-3" style="width: 30%;"><%= @contact.subject_i18n %></div>

      <div class="mt-3 ml-3" style = "text-decoration:underline;">メッセージ</div>
      <div class="mt-3 ml-3" style="white-space: pre-wrap;"><%= @contact.message %></div>


<%= form_with model: @contact, local: true do |f| %>
  <%= f.hidden_field :name %>
  <%= f.hidden_field :email %>
  <%= f.hidden_field :phone_number %>
  <%= f.hidden_field :subject %>
  <%= f.hidden_field :message %>
  <div class="mt-3 ml-1"><%= f.submit '送信' %></div>
<% end %>
<%= form_with model: @contact, url: contacts_back_path, local: true do |f| %>
  <%= f.hidden_field :name %>
  <%= f.hidden_field :email %>
  <%= f.hidden_field :phone_number %>
  <%= f.hidden_field :subject %>
  <%= f.hidden_field :message %>
  <div class="mt-2 ml-1"><%= f.submit '入力画面に戻る' %></div>
<% end %>