<div class = "container">
  <div class = "row">
    <h1>会員一覧</h1>
    <table>
      <% @customers.each do |customer| %>
      <thead>
        <td>会員ID</td>
        <td>ユーザー名</td>
        <td>メールアドレス</td>
        <td>会員ステータス</td>
      </thead>
      <tbody>
        <tr>
          <td>
            <%= customer.id %>
          </td>
          <td>
            <%= link_to admin_customer_path(customer) do %>
              <%= customer.last_name %>
              <%= customer.first_name %>
            <% end %>
          </td>
          <td>
            <%= customer.email %>
          </td>
          <% if customer.is_deleted == "retire" %>
          <td>
            <p>無効</p>
          </td>
          <% else %>
          <td>
            <p>有効</p>
          </td>
          <% end %>
        </tr>
      </tbody>
      <tfoot>
        <td>
          <%= paginate @customers %>
        </td>
      </tfoot>
      <% end %>
    </table>
  </div>
</div>
